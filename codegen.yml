overwrite: true

schema:
  - ./server/src/api/scheme.gql

generates:
  ./server/src/api/resolverType.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./client/@types/apolloType.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
